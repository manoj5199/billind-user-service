{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**"]
      }
    }
  ],
  "routes": [{ "src": "/dist/(.*)", "dest": "dist/index.js" }],
  "buildCommand": "npm run build"
}
